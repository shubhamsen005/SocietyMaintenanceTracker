'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Register(){const router=useRouter();const [form,setForm]=useState({name:'',email:'',password:'',inviteCode:''});const [message,setMessage]=useState('');const submit=async()=>{const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});if(r.ok){setMessage('Account created. You can now sign in.');setTimeout(()=>router.push('/login'),900)}else setMessage('That invite is invalid, expired, or already used.');};return <main className="login"><section className="login-form"><div><span className="section-label">RESIDENT ACCESS</span><h2>Join your society workspace</h2><p>A valid one-time invite code from your society administrator is required.</p>{[['name','Full name'],['email','Email address'],['password','Password'],['inviteCode','Invite code']].map(([key,label])=><label key={key}>{label}<div className="input"><input type={key==='password'?'password':'text'} value={form[key as keyof typeof form]} onChange={e=>setForm({...form,[key]:e.target.value})}/></div></label>)}{message&&<p className="red" role="alert">{message}</p>}<button className="primary wide" onClick={submit}>Create resident account</button><p className="signin-note">Already registered? <a href="/login">Sign in</a></p></div></section></main>}
