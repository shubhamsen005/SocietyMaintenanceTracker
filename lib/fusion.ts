export type FusionCandidate = { category:string; building:string; assetId?:string|null; description:string; openedAt:Date };
const words=(text:string)=>new Set(text.toLowerCase().replace(/[^a-z0-9 ]/g,' ').split(/\s+/).filter(word=>word.length>2));
export function scoreFusion(a:FusionCandidate,b:FusionCandidate,now=new Date()) { const reasons:string[]=[]; let score=0; if(a.category===b.category){score+=25;reasons.push('Same category');} if(a.building===b.building){score+=20;reasons.push('Same building');} if(a.assetId&&a.assetId===b.assetId){score+=25;reasons.push('Same asset');} const left=words(a.description),right=words(b.description);const shared=[...left].filter(word=>right.has(word)).length;const union=new Set([...left,...right]).size;const textScore=union?Math.round((shared/union)*20):0;if(textScore>=4){score+=textScore;reasons.push('Similar description');} const hours=Math.abs(a.openedAt.getTime()-b.openedAt.getTime())/3600000;if(hours<=2){score+=10;reasons.push('Reported within 2 hours');}else if(hours<=24)score+=5;return { confidence:Math.min(100,score), reasons, evaluatedAt:now }; }
